# Queue Storage (démo/artefacts)

Emplacement : `__queuestorage__/`

But : dossier utilisé pour stocker des artefacts liés à la queue storage (peut être un répertoire de démonstration ou d'empaquetage). Actuellement vide.

Notes :
- Si ce dossier contient des fichiers temporaires ou de test, il peut rester dans le repo ou être ignoré selon vos besoins.
- Si c'est un artefact de build, ajoutez-le au `.gitignore`.
